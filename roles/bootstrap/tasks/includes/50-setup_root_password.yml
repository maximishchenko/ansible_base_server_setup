---
- name: Always update password for root user
  ansible.builtin.user:
    name: root
    password: "{{ bootstrap_root_password_hash | string | password_hash('sha512', 65534 | random(seed=inventory_hostname) | string) }}"
    update_password: always
